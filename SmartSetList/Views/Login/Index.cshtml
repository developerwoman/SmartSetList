@{
    ViewData["Title"] = "Login";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <link rel="stylesheet" href="~/css/home.css" />

</head>
<body>
    <div>
        <div class="container text-center">
            <h1>Authorize with Spotify</h1>
            @* <button class="btn" @Html.ActionLink("Logins", "Authorize")</button> *@
            <p>@Html.ActionLink("SpotifyController", "Callback")</p>
            @* @Html.ActionLink("Este link se parece como um button", "Authorize", "Login", null, new { @class = "btn btn-primary" }) *@

            <div id="profile" class="profile-info"></div>

        </div>

    </div>


    <script>

        // const clientId = '78b1bfd41bce4f2ba43bb00dd43f2b68';  // Coloque seu client_id aqui
        // // const clientId = '78b1bfd41bce4f2ba43bb00dd43f2b6ss8';  // Coloque seu client_id aqui
        // const redirectUri = 'https://localhost:44347/Home/Privacy';  // Deve ser o mesmo URI configurado no dashboard
        // const scopes = [
        //     'playlist-read-private',
        //     'user-library-read'
        // ];

        // function loginWithSpotify() {
        //     const authEndpoint = 'https://accounts.spotify.com/authorize';
        //     const responseType = 'code';
        //     const authUrl = `${authEndpoint}?client_id=${clientId}&redirect_uri=${encodeURIComponent(redirectUri)}&scope=${scopes.join('%20')}&response_type=${responseType}`;
        //     console.log(authUrl);
        //     // Redirecionar o usuário para a página de autenticação do Spotify
        //     window.location.href = authUrl;
        // }

        // // Pegar o token de acesso da URL após o redirecionamento
        // function getAccessToken() {
        //     const hash = window.location.hash
        //         .substring(1)
        //         .split('&')
        //         .reduce(function (initial, item) {
        //             if (item) {
        //                 var parts = item.split('=');
        //                 initial[parts[0]] = decodeURIComponent(parts[1]);
        //             }
        //             return initial;
        //         }, {});

        //     window.location.hash = '';  // Limpa o hash para não exibir o token na URL
        //     return hash.access_token;
        // }

        // // Pegar os dados do usuário autenticado
        // function getUserProfile(token) {
        //     fetch('https://api.spotify.com/v1/me', {
        //         headers: {
        //             'Authorization': 'Bearer ' + token
        //         }
        //     })
        //         .then(response => response.json())
        //         .then(data => {
        //             document.getElementById('profile').innerHTML = `
        //                                     <h3>Bem-vindo, ${data.display_name}!</h3>
        //                                     <p>Email: ${data.email}</p>
        //                                     <img src="${data.images[0]?.url}" alt="Profile Image" width="100" height="100">
        //                                 `;
        //         })
        //         .catch(error => {
        //             console.error('Erro ao buscar perfil:', error);
        //         });
        // }

        // // Após o redirecionamento, obter o token e pegar os dados do usuário
        // window.onload = function () {
        //     const token = getAccessToken();
        //     if (token) {
        //         getUserProfile(token);
        //     }
        // }
    </script>
</body>
</html>